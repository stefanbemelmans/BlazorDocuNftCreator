@inherits TokenInfoModel

<div class="flex-column justify-content-center m-2 p-2">
  <h3 class="text-center">Token Info</h3>
  @*@if (TokenDataList.Count > 0)
    {
      <select @onchange=@SetNewToken>
        @foreach (var token in TokenDataList)
        {
          <option value=@token.TokenId>Token Id</option>
        }
      </select>
    }*@
  <div class="container bg-danger">
    <div class="row text-center">
      <p>Template Info</p>
      <div class="col">
        <h4>Name: @CurrentToken.TemplateData.Name</h4>
      </div>
      <div class="col">
        <h4>Symbol: @CurrentToken.TemplateData.Symbol</h4>
      </div>
      <div class="col">
        <h4>MintLimit: @CurrentToken.TemplateData.MintLimit</h4>
      </div>
      <div class="col">
        <h4>AttachedTokens: @CurrentToken.TemplateData.AttachedTokens</h4>
      </div>
    </div>
  </div>
  <div class="container bg-info mt-4">
    <div class="row">
      <p>Token Info</p>
      @*<div class="col text-center">
        <h4>Token Id: @CurrentToken.TokenId</h4>
      </div>
      <div class="col text-center">
        <h4>Balance: @CurrentToken.Balance</h4>
      </div>*@
    </div>
    <div class="row">
      Token Id and stuff
      @*@if (CurrentToken.TokenId == 3)
      {

        <div class="col text-center">
          <h4>Date:</h4>
          <h5 class="bg-light">@CurrentToken.ImmDataObj.Date</h5>
        </div>
        <div class="col text-center">
          <h4>Minted From:</h4>
          <h5 class="bg-light">@CurrentToken.ImmDataObj.MintedFrom</h5>
        </div>
        <div class="col text-center">
          <h4>Title:</h4>
          <h5 class="bg-light">@CurrentToken.ImmDataObj.Title</h5>
        </div>
      }*@
      @*@if (CurrentToken.Data != null)
      {
        <div class="col text-center">
          <h4>Immutable Data:</h4>
          <h5 class="bg-light">@CurrentToken.Data</h5>
        </div>
      }
      @if (CurrentToken.MutableData != null)
      {
        <div class="col text-center">
          <h4>Immutable Data:</h4>
          <h5 class="bg-light">@CurrentToken.MutableData</h5>
        </div>
      }*@
    </div>
  </div>
</div>
